/**
 * A very simple shell. This is a stopgap solution to making
 * the OS interactive before processes are implemented.
 */
#ifndef DIAG_SHELL_H
#define DIAG_SHELL_H

void shell_init(void);
void shell_on_interrupt(void);

#endif // DIAG_SHELL_H
